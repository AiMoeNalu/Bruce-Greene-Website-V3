import React, { Fragment } from 'react';
import './Header.scss';

const Header = (props) => {
    return(
    <Fragment>
        <div id='header'>
            Header
        </div>
    </Fragment>
    )
}

export default Header;